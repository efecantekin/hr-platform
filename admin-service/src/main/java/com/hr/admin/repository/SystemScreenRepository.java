package com.hr.admin.repository;

import com.hr.admin.entity.SystemScreen;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SystemScreenRepository extends JpaRepository<SystemScreen, Long> {
}